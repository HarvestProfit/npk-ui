import { Canvas, Meta, ArgTypes } from '@storybook/addon-docs/blocks';
 
import * as Stories from './Grid.stories.jsx';
 
<Meta of={Stories} />
 
# Grid

The grid role can be used to create a grid. Grids allow users to click on them and often show an array of data.
Below is a simple breakdown table of different yields to prices. 

<Canvas of={Stories.Grid} />

## Props

<ArgTypes of={Stories} />
