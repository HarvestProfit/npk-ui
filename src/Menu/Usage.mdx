import { Canvas, Meta, Controls } from '@storybook/blocks';
 
import * as MenuStories from './Menu.stories.jsx';
 
<Meta of={MenuStories} />
 
# Menus


### Basic Dialog
<small>variant: <code>dialog</code></small>

Produces a dialog menu that has more robust buttons and icons for more visual communication.
<Canvas of={MenuStories.BasicDialog} />

### Basic Menu
<small>variant: <code>menu</code></small>

Smaller menu that is better for fewer actions.
<Canvas of={MenuStories.BasicMenu} />

### Basic Select
<small>variant: <code>select</code></small>

Produces a select menu useful for selecting an option from a list.
<Canvas of={MenuStories.BasicSelect} />

### Plain Menu
You don't need to have a list of buttons in a menu, it can contain anything you want.
<Canvas of={MenuStories.CustomMenu} />

### Sub Menus
Allows you to create more complicated menu structures by nesting options.
<Canvas of={MenuStories.SubMenus} />

## Props

<Controls />