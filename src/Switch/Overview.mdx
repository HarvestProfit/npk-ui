import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
 
import * as SwitchStories from './Switch.stories.jsx';
 
<Meta of={SwitchStories} />
 
# Switches

Switches toggle between two states, on and off. They should be used when the user needs to see the current state of a setting, and be able to change it.
These are often the better choice over checkboxes for settings that are enabled or disabled.
 
<Canvas of={SwitchStories.Default} />


## Props

<Controls />